"""flowbridge: a lightweight automation bridge between tools.

Receives an event over a webhook, validates it, applies configurable
transforms, and dispatches it to one or more pluggable actions.
"""

__version__ = "0.1.0"
